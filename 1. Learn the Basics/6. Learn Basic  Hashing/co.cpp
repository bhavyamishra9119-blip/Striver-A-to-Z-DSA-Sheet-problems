#include <bits/stdc++.h>
using namespace std;

int main() 
{
    vector<int> Array;
    int n;

    for (int i=0; i<n; i++)
    {
        cout << "The elementb at " << i << " is: ";
        cin >> Array;
        cout << Array[i];
    }
    
    return 0;
}